document.addEventListener('DOMContentLoaded', () => {
    // Check if the hero element exists to verify the background is ready
    const hero = document.querySelector('.hero');
    
    if (hero) {
        console.log("ZNN Animated GIF Background Initialized.");
    }
});
